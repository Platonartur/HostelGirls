@page "/voting"
@inherits GirlsListBase
<h3>Voting</h3>






<head>
    <title>Telochki</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>


        a {
            color: red; /* Цвет ссылок */
        }

            A:visited {
                color: #cecece; /* Цвет посещенных ссылок */
            }

            A:active {
                color: #ffff00; /* Цвет активных ссылок */
            }

        .fig {
            text-align: center; /* Выравнивание по центру */
        }

        /*ul {
            list-style: none;
        }*/

        /*ul li:before {
                content: "";
                font-size: 170%; or whatever
                padding-right: 5px;
            }
        */

    </style>
    <style>
        .header-h1 {
            text-align: center;
            margin-bottom: .5rem;
        }

        /*.header-h1 h1 {
                display: inline-block;
                background: #0000CD; #0097a7
                color: #FFFFFF;
                margin-bottom: 0;
                padding: .5rem 1rem .625rem 1rem;
                font-size: 2rem;
                text-transform: uppercase;
                border-radius: 30px;
            }
                */
    </style>
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
</head>






<link href="/css/styles.css" rel="stylesheet" type="text/css">










@*@code {
    void Random()
    {
        var random = new Random();
        var teens1 = teens.OrderBy(order => random.Next());
        foreach (var teen in teens)
        {

        }
    }
    public Random Rew()
    {
        var random = new Random();
        var teens1 = teens.OrderBy(order => random.Next());

    }
}*@
<div>
    @for (int i = 0; i < 100; i++)
    {
        //IEnumerator ie = teens.GetEnumerator();
        var rand = new Random();
        //LoadTeens(random);
        //int x = random.Next(1,13);
        //var teens1 = teens.ToList();
        //var teens2 = teens1.OrderBy(order => random.Next());

        //OnInitializedAsync().Id;
        //Random rand = new Random();
        //int filename = rand.Next(1, 13);
        //string image = teen.ImgName + ".jpg";
        var teens1 = teens.OrderBy(order => rand.Next());
        string Randomize()
        {
            foreach (var teen in teens1)
            {
                return teen.Img;
            }
            return "xyi";
        }
        foreach (var teen in teens1)
        {
            <div>
                <div class="card m-3" style="min-width: 18rem; max-width: 30.5%;">
                    <div class="card-header">
                        <h3><a href="@teen.Id">@teen.Name</a> </h3>
                        <p>
                            <input type="image" img class="card-img-top" src="@teen.Img"
                                   onclick="">
                        </p>
                        <div class="card-footer text-center">
                            <a href="#" bgcolor="red" class="btn-primary m-1">  View</a>
                            <a href="#" class="btn-primary m-1">Edit</a>
                            <a href="#" class="btn-primary m-1">Delete</a>
                            <button> lox</button>
                            imagebutton
                        </div>
                    </div>
                </div>
            </div>
        }

        @*<div>
                    <div class="card m-3" style="min-width: 18rem; max-width: 30.5%;">
                        <div class="card-header">
                            <h3><a href="@teen.Id">@teen.Name</a> </h3>
                            <p><input type="image" img class="card-img-top" src="@teen.Img"
                                      onclick="@Randomize()"></p>
                            <div class="card-footer text-center">
                                <a href="#" bgcolor="red" class="btn-primary m-1">  View</a>
                                <a href="#" class="btn-primary m-1">Edit</a>
                                <a href="#" class="btn-primary m-1">Delete</a>
                                <button> lox</button>
                                imagebutton
                            </div>
                        </div>
                    </div>
            </div>*@
        //<div> <a href="#"></a></div>
        //for (int)
        //var Image = new Image(path + filename + ".jpg");
        //   <div>
        //       <a href="#">@</a>
        //   </div>
    }
</div>















































@*<div>
    @for (int i = 0; i < 100; i++)
    {
        //IEnumerator ie = teens.GetEnumerator();
        var random = new Random();
        //LoadTeens(random);
        //int x = random.Next(1,13);
        //var teens1 = teens.ToList();
        //var teens2 = teens1.OrderBy(order => random.Next());

        //OnInitializedAsync().Id;
        //Random rand = new Random();
        //int filename = rand.Next(1, 3);
        //string image = teen.ImgName + ".jpg";

        var teens1 = teens.OrderBy(order => random.Next());
        <div>
            @foreach (var teen in teens1)
            {
                <div class="card m-3" style="min-width: 18rem; max-width: 30.5%;">
                    <div class="card-header">
                        <h3><a href="@teen.Id">@teen.Name</a> </h3>
                        <p><input type="image" img class="card-img-top" src="@teen.Img" onclick="window.location.href = '@teen.Id';"></p>
                        <div class="card-footer text-center">
                            <a href="#" bgcolor="red" class="btn-primary m-1">  View</a>
                            <a href="#" class="btn-primary m-1">Edit</a>
                            <a href="#" class="btn-primary m-1">Delete</a>
                            <button> lox</button>
                            imagebutton
                        </div>
                    </div>
                </div>
            }
        </div>
        //<div> <a href="#"></a></div>
        //for (int)
        //var Image = new Image(path + filename + ".jpg");
        //   <div>
        //       <a href="#">@</a>
        //   </div>
    }
</div>*@